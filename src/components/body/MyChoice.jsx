import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyChoice = () => {

    useTitle("My Choice")

    return (
        <div>

        </div>
    );
};

export default MyChoice;